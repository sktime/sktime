# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

version: 2

python:
  version: 3.7
  install:
    - requirements: docs/requirements.txt  # install requirements
    - method: setuptools  # install package itself
      path: sktime

sphinx:
  configuration: docs/source/conf.py

formats:
  - pdf
